<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GamingJr" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryDark</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondaryDark</item>
        <item name="colorOnSecondary">@color/onSecondary</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="android:statusBarColor">@color/primaryDark</item>
        <item name="android:navigationBarColor">@color/primaryDark</item>
        <item name="android:colorButtonNormal">@color/primary</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Makes status bar icons dark -->
        <item name="android:windowLightNavigationBar">true</item> <!-- Makes navigation bar icons dark -->
    </style>

    <style name="Theme.GamingJr" parent="Base.Theme.GamingJr" />

    <!-- Estilo de botones -->
    <style name="Widget.Button.GamingJr" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/primary</item>
        <item name="cornerRadius">16dp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Estilo de las esquinas redondeadas -->
    <style name="ShapeAppearanceOverlay.App.CornerSizeMedium" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>

    <!-- Estilo de los textos -->
    <style name="TextAppearance.GamingJr.Headline" parent="TextAppearance.Material3.HeadlineLarge">
        <item name="android:textColor">@color/primaryDark</item>
    </style>

    <style name="TextAppearance.GamingJr.Body" parent="TextAppearance.Material3.BodyLarge">
        <item name="android:textColor">@color/black</item>
    </style>
</resources>
